<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="du_cs.dongmatgol">
    <application
        android:allowBackup="true"
        android:icon="@drawable/dmg_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:theme="@android:style/Theme.Light.NoTitleBar"/>
        <activity
            android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainPage"/>
        <activity android:name=".MenuPage"/>
        <activity android:name=".RandomPage"/>
        <activity android:name=".RandomMenuPage"/>
        <activity android:name=".Membershipjoin"/>
        <activity android:name=".Membershiplistview"/>
    </application>

</manifest>
